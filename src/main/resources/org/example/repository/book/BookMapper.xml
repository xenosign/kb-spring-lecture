<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.repository.book.BookMapper">
    <select id="findAll" resultType="Book">
        select * from bookMybatis
    </select>

    <insert id="save" useGeneratedKeys="true" keyProperty="id">
        insert into bookMybatis (title, author)
        values (#{title}, #{author})
    </insert>

    <delete id="delete">
        delete from bookMybatis where id = #{id}
    </delete>
</mapper>